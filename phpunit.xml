<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.1/phpunit.xsd" bootstrap="vendor/autoload.php" colors="true" cacheDirectory=".phpunit.cache">
  <testsuites>
    <testsuite name="Feature">
      <directory suffix="Test.php">./tests/Feature</directory>
    </testsuite>
  </testsuites>
  <coverage/>
  <php>
    <server name="APP_ENV" value="testing"/>
    <server name="DB_DRIVER" value="sqlite"/>
    <server name="DB_HOST" value=""/>
    <server name="DB_PORT" value="0"/>
    <server name="DB_USERNAME" value=""/>
    <server name="DB_PASSWORD" value=""/>
    <server name="DB_DATABASE" value=":memory:"/>
  </php>
  <source>
    <include>
      <directory suffix=".php">./src</directory>
    </include>
    <exclude>
      <!-- Blade views are compiled to storage/framework/views before execution,
           so Xdebug cannot map their coverage back to the source .blade.php files.
           They are template markup, not unit-testable logic — exclude them so the
           report reflects real PHP coverage. -->
      <directory suffix=".blade.php">./src</directory>
    </exclude>
  </source>
</phpunit>
